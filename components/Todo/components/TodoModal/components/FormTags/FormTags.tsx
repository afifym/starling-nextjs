import React from 'react';
import styled from 'styled-components';

interface IProps {}

const FormTags: React.FC<IProps> = () => {
  return <Wrapper>Form Tags</Wrapper>;
};

export default FormTags;

const Wrapper = styled.div``;
