import React from 'react';
import styled from 'styled-components';

interface IProps {}

const FormAccent: React.FC<IProps> = () => {
  return <Wrapper>Form Accent</Wrapper>;
};

export default FormAccent;

const Wrapper = styled.div``;
